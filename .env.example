# Server Configuration
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sanjeevani_erp?schema=public"

# Authentication (JWT)
JWT_SECRET="changeme_in_production_super_secure_key"
JWT_EXPIRES_IN="1d"

# Shopee Isolation (Optional for Local Dev, Required for Prod)
CURRENT_SHOPEE_ID=1
